// Lógica de negócio pura — preenchido a partir da Fase 1.
// §4 do prompt: este package não tem dependências de framework HTTP nem de Prisma.
// Reutilizável entre backend, ai-service e jobs.
export const DOMAIN_PACKAGE_READY = true;
